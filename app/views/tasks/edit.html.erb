<h1>タスクID: <%= @task.id %>を編集</h1>

<%= form_with(model: @task, local: true) do |f| %>

  <%= f.label :content, "タスク名" %>
  <%= f.text_field :content %>
  
  <%= f.submit "登録" %>

<% end %>

<%= link_to "タスク一覧を表示", tasks_path %>