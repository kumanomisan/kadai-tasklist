<h1>タスク一覧</h1>

<ul>
    <% @tasks.each do |task| %>
      <li>ID:<%= task.id %> <%= link_to task.content, task %> 状態: <%= task.status %></li>
    <% end %>
</ul>

<%= link_to "新規タスク登録を行う", new_task_path %>
